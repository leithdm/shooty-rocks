describe("test the canvas element", () => {
    it("should have setupCanvas() method defined", () => {
        expect(setupCanvas).toBeDefined();
      });
    it("should have render() method defined", () => {
        expect(render).toBeDefined();
      });
}); 
describe("test keyboard input", () => {
    it("should have setupKeyboardInput() method defined", () => {
        expect(setupKeyboardInput).toBeDefined();
      });
}); 